node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit

# The build output is not tracked, but the directory is: internal/api embeds it, so a Go
# build on a fresh checkout needs it to exist before `npm run build` has ever run.
/build/*
!/build/.gitignore

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
