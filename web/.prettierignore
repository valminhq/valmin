# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Copied in from shadcn-svelte, not written here (ADR-002). Keeping them byte-identical to
# upstream is what makes a re-run of the CLI a readable diff rather than a reformat.
src/lib/components/ui/
